<%= link_to users_path(notification.notifiable.slug, anchor: dom_id(notification.notifiable)), class: "dropdown-item #{"unread" if !notification.read_at?}" do %>
  <%= notification.actor.username %>
  started <%= notification.action %> you
  <%#= notification.notifiable_type.underscore.humanize.downcase %>
<% end %>