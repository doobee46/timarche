<% if user_signed_in? %>
<%= render :partial => 'shared/signedmenu'%>
<%else%>
<div class="container">
    <div class="row">
      <div class="col-md-12">
        <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
          <div class="container">
            <div class="row">
              <div class="col-md-12">
                <div class="navbar-header">
                  <div class="navbar-brand" href="#">
                    <%= link_to image_tag("reverse_logo.png", class: ' animated bounce'), root_path %>
                  </div>
                </div>
                 <ul class="nav navbar-nav pull-right">
                  <li><%=link_to "Publications", listings_path %></li>
                  <li>
                    <%= link_to "S'inscrire",user_registration_path %> <span class="sr-only">(current)</span>
                    </a>
                  </li>
                  <li>
                    <%= link_to "Se connecter",user_session_path %> 
                  </li>
                  <li class="dropdown">
                     <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Menu <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        <li>
                          <%= link_to about_path, data: { no_turbolink: true } do %>
                            <i class="glyphicon glyphicon-info-sign"></i> A propos
                          <% end %> 
                        </li>
                         <li>
                          <%= link_to contact_path, data: { no_turbolink: true } do %>
                            <i class="glyphicon glyphicon-phone-alt"></i> Contact Us
                          <% end %> 
                        </li>
                    </ul>
                  </li>
                  <%if user_signed_in?%>
                  <li>
                    <button type="button" class="btn btn-post" data-toggle="modal" data-target="#myModal">
                      Launch demo modal
                    </button>
                   
                  </li>
                  <% end %>
                </ul>
              </div>
            </div>                
          </div>
        </nav>
      </div>         
    </div>
</div>
<%end%>