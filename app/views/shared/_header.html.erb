<% if user_signed_in? %>
<%= render :partial => 'shared/signedmenu'%>
<%else%>
<div class="container">
    <div class="row">
      <div class="col-md-12">
        <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
          <div class="container">
            <div class="row">
              <div class="col-md-12">
                <div class="navbar-header">
                  <div class="navbar-brand" href="#">
                    <%= link_to image_tag("reverse_logo.png", class: ' animated bounce'), root_path %>
                  </div>
                </div>
                 <ul class="nav navbar-nav pull-right">
                  <li class="active">
                    <%= link_to "S'inscrire",new_user_registration_path %> <span class="sr-only">(current)</span>
                    </a>
                  </li>
                  <li>
                    <%= link_to "Se Connecter",new_user_session_path %> 
                  </li>
                  <li class="dropdown">
                     <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Menu <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        <li>
                          <%= link_to '', data: { no_turbolink: true } do %>
                            <i class="glyphicon glyphicon-info-sign"></i> A propos
                          <% end %> 
                        </li>
                         <li>
                          <%= link_to '', data: { no_turbolink: true } do %>
                            <i class="glyphicon glyphicon-phone-alt"></i> Contact Us
                          <% end %> 
                        </li>
                    </ul>
                  </li>
                  <%if user_signed_in?%>
                  <li>
                    <%= button_to "Deposer une annonce", new_listing_path, :method => :get, class: 'btn btn-post'%>
                  </li>
                  <% end %>
                </ul>
              </div>
            </div>                
          </div>
        </nav>
      </div>         
    </div>
</div>
<%end%>