 <div class="container">
    <div class="row">
      <div class="col-md-12">
        <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
          <div class="container">
            <div class="row">
              <div class="col-md-12">
                <div class="navbar-header">
                  <div class="navbar-brand" href="#">
                    <%= link_to image_tag("reverse_logo.png", class: ' animated bounce'), root_path %> 
                  </div>
                </div>
                  <ul class="nav navbar-nav pull-right">
                    <% unless @title == "browse" %>
                    <li><%=link_to "Nos annonces", browse_path , class: 'brow'%></li>
                    <%end%>
                    <li>
                       <%= link_to conversations_path, data: { no_turbolink: true } do %>
                          <i class="glyphicon glyphicon-envelope"><span class="badge"><%=current_user.mailbox.inbox({:read => false}).count%></span></i>
                        <%end%>
                    </li>
                    <li class="dropdown">
                       <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                        <span class="about-image">
                          <%= image_tag current_user.avatar.url(:athumb), class: "img-circle",size: "30x30" %>
                        </span>
                        <span class="caret"></span>
                      </a>
                      <ul class="dropdown-menu" role="menu">
                        <li>
                          <%= link_to '', data: { no_turbolink: true } do %>
                            <i class="glyphicon glyphicon-tasks"></i> Envoyer Message
                          <% end %> 
                        </li>
                        <li>
                          <%= link_to edit_user_registration_path, data: { no_turbolink: true } do %>
                            <i class="glyphicon glyphicon-cog"></i> Settings
                          <% end %> 
                        </li>
                        <% if current_user.listings.exists?%>
                        <li>
                          <%= link_to seller_path, data: { no_turbolink: true } do %>
                            <i class="glyphicon glyphicon-tasks"></i> Admin
                          <% end %> 
                        </li>
                        <%end%>
                        <li>
                          <%= link_to destroy_user_session_path, method: :delete, data: { no_turbolink: true } do %>
                            <i class="glyphicon glyphicon-log-out"></i> Déconnexion
                          <% end %> 
                        </li>
                      </ul>     
                    </li>
                    <li class="dropdown">
                       <a href="#" class="dropdown-toggle brow" data-toggle="dropdown" role="button" aria-expanded="false">Menu <span class="caret "></span></a>
                      <ul class="dropdown-menu" role="menu">
                          <li>
                            <%= link_to about_path , data: { no_turbolink: true } do %>
                              <i class="glyphicon glyphicon-info-sign"></i> A propos
                            <% end %> 
                          </li>
                           <li>
                            <%= link_to contact_path, data: { no_turbolink: true } do %>
                              <i class="glyphicon glyphicon-phone-alt"></i> Contact Us
                            <% end %> 
                          </li>
                      </ul>
                    </li>
                    <li>
                      <button type="button" class="btn btn-post" data-toggle="modal" data-target="#myModal">Poster des Annonces</button>                  
                    </li>
                  </ul>
              </div>
            </div>                
          </div>
        </nav>
      </div>         
    </div>
</div>
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="myModalLabel">Modal title</h4>
      </div>
      <div class="modal-body">
    
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Save changes</button>
      </div>
    </div>
  </div>
</div>