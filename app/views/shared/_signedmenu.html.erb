 <div class="container">
    <div class="row">
      <div class="col-md-12">
        <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
          <div class="container">
            <div class="row">
              <div class="col-md-12">
                <div class="navbar-header">
                  <div class="navbar-brand" href="#">
                    <%= link_to image_tag("logo.png", class: ' animated bounce'), main_app.root_path %> 
                  </div>
                </div>
                  <ul class="nav navbar-nav pull-right">
                    <% unless @title == "browse" %>
                    <li><%=link_to "Publications", main_app.browse_path , class: 'brow'%></li>
                    <%end%>
                    <li>
                       <%= link_to main_app.conversations_path, data: { no_turbolink: true } do %>
                          <i class="glyphicon glyphicon-envelope"><span class="badge"><%=current_user.mailbox.inbox({:read => false}).count%></span></i>
                        <%end%>
                    </li>
                    <li class="dropdown">
                       <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                        <span class="about-image">
                          <%= image_tag current_user.avatar.url(:athumb), class: "img-circle",size: "30x30" %>
                        </span>
                        <span class="caret"></span>
                      </a>
                      <ul class="dropdown-menu" role="menu">
                        <li>
                          <%= link_to '', data: { no_turbolink: true } do %>
                            <i class="glyphicon glyphicon-tasks"></i> Envoyer Message
                          <% end %> 
                        </li>
                        <li>
                          <%= link_to main_app.edit_user_registration_path, data: { no_turbolink: true } do %>
                            <i class="glyphicon glyphicon-cog"></i> Settings
                          <% end %> 
                        </li>
                        <li>
                          <%= link_to main_app.seller_path, data: { no_turbolink: true } do %>
                            <i class="glyphicon glyphicon-tasks"></i> Dashboard
                          <% end %> 
                        </li>
                        <li>
                          <%= link_to  koudoku.owner_subscriptions_path(current_user), data: { no_turbolink: true } do %>
                            <i class="glyphicon glyphicon-tasks"></i> Dashboard
                          <% end %> 
                        </li>
                        <li>
                          <%= link_to main_app.destroy_user_session_path, method: :delete, data: { no_turbolink: true } do %>
                            <i class="glyphicon glyphicon-log-out"></i> Déconnexion
                          <% end %> 
                        </li>
                      </ul>     
                    </li>
                    <li class="dropdown">
                       <a href="#" class="dropdown-toggle brow" data-toggle="dropdown" role="button" aria-expanded="false">Menu <span class="caret "></span></a>
                      <ul class="dropdown-menu" role="menu">
                          <li>
                            <%= link_to main_app.about_path , data: { no_turbolink: true } do %>
                              <i class="glyphicon glyphicon-info-sign"></i> A propos
                            <% end %> 
                          </li>
                           <li>
                            <%= link_to main_app.contact_path, data: { no_turbolink: true } do %>
                              <i class="glyphicon glyphicon-phone-alt"></i> Contact Us
                            <% end %> 
                          </li>
                      </ul>
                    </li>
                   <% if @title == "seller"%>
                    <li>
                      <button type="button" class="btn btn-post" data-toggle="modal" data-target="#myModal">Poster des Annonces</button>                  
                    </li>
                  <%end%>
                  </ul>
              </div>
            </div>                
          </div>
        </nav>
      </div>         
    </div>
</div>
